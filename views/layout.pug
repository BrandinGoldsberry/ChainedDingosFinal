html
    head
        link(href='style.css', rel='stylesheet')

    body
        header
            nav
                ul(class='menu')
                    each item in config.menu
                        a(href=item[1])
                            div(class='menuItem')
                                li(id=item[0]) #{item[0]} 
        section
            block content
        footer